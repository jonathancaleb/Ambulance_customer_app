import 'package:flutter/material.dart';

class DriverController extends StatelessWidget {
  const DriverController({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
